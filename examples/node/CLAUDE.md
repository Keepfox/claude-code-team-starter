# CLAUDE.md

## Node Project Defaults

- Prefer `npm run lint`, `npm run test`, and `npm run build` when scripts exist.
- Prefer the smallest relevant script before full-suite runs.
- When editing TypeScript, keep types explicit when the code would become ambiguous.
- Update README or package scripts docs when developer workflow changes.
